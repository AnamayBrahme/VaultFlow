apiVersion: v1
kind: Service
metadata:
  name: {{ include "vaultflow-observability.fullname" . }}
  labels:
    {{- include "vaultflow-observability.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vaultflow-observability.selectorLabels" . | nindent 4 }}
