global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'vaultflow-api-custom'
    static_configs:
      - targets: ['host.docker.internal:8443'] # ◄ Directs Docker to look out onto your machine's proxy